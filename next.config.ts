import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // images.remotePatterns is populated when prize NFT media lands — until the
  // raffle pages exist there is no remote host to allow yet.
};

export default nextConfig;
